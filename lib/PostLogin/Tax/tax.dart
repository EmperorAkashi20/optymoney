import 'package:flutter/material.dart';
import 'package:optymoney/PostLogin/Tax/Components/body.dart';

class Tax extends StatelessWidget {
  static String routeName = '/tax';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
