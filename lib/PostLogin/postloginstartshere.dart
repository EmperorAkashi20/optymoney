import 'package:flutter/material.dart';

import 'dashboard/dashboard.dart';

class PostLoginStartsHere extends StatelessWidget {
  static String routeName = '/beginning';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: DashBoard(),
    );
  }
}
