import 'package:flutter/material.dart';
import 'package:optymoney/PinSetup/Components/Body1.dart';

class FinalScreen extends StatelessWidget {
  static String routeName = "/finalScreen";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Body(),
    );
  }
}
